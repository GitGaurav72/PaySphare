spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/kyvos}
    username: ${DB_USERNAME:kyvos}
    password: ${DB_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-key-please-override-with-env-var-min-256-bits-long}

logging:
  level:
    com.PaySphere: INFO
    org.hibernate.SQL: WARN
