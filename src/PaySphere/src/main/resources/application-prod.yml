spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-da0as08u01pc738omb30-a.virginia-postgres.render.com/paysphere}
    username: ${DB_USERNAME:paysphere}
    password: ${DB_PASSWORD:tcgmBqHJUHNOFUE6Fu9qhB4xyh4jVKju}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-key-please-override-with-env-var-min-256-bits-long}

logging:
  level:
    com.PaySphere: INFO
    org.hibernate.SQL: WARN
